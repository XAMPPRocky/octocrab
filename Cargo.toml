# Fix for Issue #224: Octocrab doesn't compile with Yew (for me)

pub fn instance() -> Arc<Octocrab> {
    STATIC_INSTANCE.load().clone()
}

/// Initialises the static instance with the configuration set by `builder`.
///
/// # Example
///